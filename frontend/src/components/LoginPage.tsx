"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { useApp } from "@/lib/store";
export function LoginPage(){ const { t, login, register, loginDemoAdmin, loginDemoCustomer }=useApp(); const router=useRouter(); const [mode,setMode]=useState<'login'|'register'>('login'); const [form,setForm]=useState({email:'admin@luxora.dev',password:'admin12345',displayName:''}); const [error,setError]=useState(''); const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');try{mode==='login'?await login(form.email,form.password):await register(form);router.push('/profile');}catch(err){setError(err instanceof Error?err.message:'Failed');}}; const demoAdmin=()=>{loginDemoAdmin();router.push('/admin')}; const demoCustomer=()=>{loginDemoCustomer();router.push('/profile')}; return <main className="px-4 py-16"><div className="mx-auto max-w-md rounded-[2rem] border bg-white p-8 shadow-xl"><h1 className="text-3xl font-black">{mode==='login'?t('login'):t('register')}</h1><div className="mt-5 grid gap-3 sm:grid-cols-2"><button onClick={demoAdmin} className="rounded-full bg-stone-950 px-4 py-3 font-bold text-white">{t('useDemoAdmin')}</button><button onClick={demoCustomer} className="rounded-full border px-4 py-3 font-bold">{t('useDemoCustomer')}</button></div><form onSubmit={submit} className="mt-6 space-y-4">{mode==='register'?<input className="input" placeholder={t('fullName')} value={form.displayName} onChange={e=>setForm({...form,displayName:e.target.value})}/>:null}<input className="input" type="email" placeholder={t('email')} value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input className="input" type="password" placeholder={t('password')} value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button className="w-full rounded-full bg-amber-800 px-6 py-4 font-black text-white">{mode==='login'?t('login'):t('register')}</button></form><button onClick={()=>setMode(mode==='login'?'register':'login')} className="mt-4 text-sm font-bold text-amber-800">{mode==='login'?'Create customer account':'I already have account'}</button>{error?<p className="mt-4 rounded-2xl bg-red-50 p-4 text-sm font-bold text-red-600">{error}</p>:null}</div></main>; }
