"use client";
import Link from "next/link";
import { ShoppingBag, Star } from "lucide-react";
import { ProductImage } from "@/components/ProductImage";
import { productMaxPrice, productMinPrice } from "@/lib/helpers";
import { useApp } from "@/lib/store";
import type { Product } from "@/types";
export function ProductCard({ product }: { product: Product }) { const { text, t, money, addToCart, getCategory }=useApp(); const min=productMinPrice(product), max=productMaxPrice(product), size=product.sizes[0]; return <article className="group overflow-hidden rounded-[2rem] border border-amber-950/10 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><Link href={`/products/${product.id}`} className="block"><div className="relative m-3 h-64 overflow-hidden rounded-[1.5rem]"><ProductImage src={product.images[0]} alt={text(product.name)}/><div className="absolute inset-0 bg-gradient-to-b from-white/10 to-stone-950/35"/>{product.featured?<span className="absolute left-4 top-4 rounded-full bg-white/90 px-3 py-1 text-xs font-bold text-amber-800"><Star className="inline h-3 w-3 fill-amber-500 text-amber-500"/> Featured</span>:null}<div className="absolute bottom-4 left-4 right-4 rounded-2xl bg-white/85 p-4 backdrop-blur-md"><p className="text-xs font-bold uppercase tracking-[.22em] text-amber-800">{product.brand}</p><h3 className="mt-1 text-xl font-black text-stone-950">{text(product.name)}</h3></div></div></Link><div className="space-y-4 p-5"><div className="flex flex-wrap gap-2"><span className="rounded-full bg-amber-50 px-3 py-1 text-xs font-bold text-amber-900">{text(getCategory(product.categoryId)?.name)}</span><span className="rounded-full bg-stone-100 px-3 py-1 text-xs font-bold text-stone-700">{t(product.gender)}</span><span className="rounded-full bg-stone-100 px-3 py-1 text-xs font-bold text-stone-700">{t(product.scentFamily)}</span></div><p className="line-clamp-2 min-h-12 text-sm leading-6 text-stone-600">{text(product.description)}</p><p className="text-2xl font-black text-stone-950">{min===max?money(min):`${money(min)} - ${money(max)}`}</p><div className="flex gap-2"><button onClick={()=>size&&addToCart(product.id,size.label)} className="flex-1 rounded-full bg-stone-950 px-4 py-3 text-sm font-bold text-white hover:bg-amber-800"><ShoppingBag className="inline h-4 w-4"/> {t('addToCart')}</button><Link href={`/products/${product.id}`} className="rounded-full border border-stone-200 px-4 py-3 text-sm font-bold">{t('viewDetails')}</Link></div></div></article>; }
